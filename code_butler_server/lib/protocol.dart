import 'package:serverpod/serverpod.dart';
import 'package:code_butler_server/src/protocol/protocol.dart';

class Protocol extends EndpointDispatch {
  @override
  void initializeEndpoints(Server server) {
    // Protocol endpoints are automatically registered
  }
}

